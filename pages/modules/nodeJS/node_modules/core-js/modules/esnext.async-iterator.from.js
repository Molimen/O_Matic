'use strict';
var $ = require('core-js/internals/export');
var toObject = require('core-js/internals/to-object');
var isPrototypeOf = require('core-js/internals/object-is-prototype-of');
var getAsyncIteratorFlattenable = require('core-js/internals/get-async-iterator-flattenable');
var AsyncIteratorPrototype = require('core-js/internals/async-iterator-prototype');
var WrapAsyncIterator = require('core-js/internals/async-iterator-wrap');
var IS_PURE = require('core-js/internals/is-pure');

// `AsyncIterator.from` method
// https://github.com/tc39/proposal-async-iterator-helpers
$({ target: 'AsyncIterator', stat: true, forced: IS_PURE }, {
  from: function from(O) {
    var iteratorRecord = getAsyncIteratorFlattenable(typeof O == 'string' ? toObject(O) : O);
    return isPrototypeOf(AsyncIteratorPrototype, iteratorRecord.iterator)
      ? iteratorRecord.iterator
      : new WrapAsyncIterator(iteratorRecord);
  }
});
